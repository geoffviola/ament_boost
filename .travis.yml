sudo: required

services:
    - docker

os:
    - linux

script:
    - pwd
    - whoami
    - docker --version
    - docker run -it osrf/ros2 /bin/bash
    - cd
    - mkdir -p temp_ws/src
    - cd temp_ws/src
    - git clone https://github.com/geoffviola/ros2_boost
    - cd ..
    - ament build
