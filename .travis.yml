sudo: required

services:
    - docker

os:
    - linux

script:
    - docker pull osrf/ros2
    - docker run -d -p 127.0.0.1:80:4567 osrf/ros2 /bin/bash
    - cd
    - mkdir -p temp_ws/src
    - cd temp_ws/src
    - git clone https://github.com/geoffviola/ros2_boost
    - cd ..
    - ament build
